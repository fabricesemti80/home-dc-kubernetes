---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: uptime-kuma
  namespace: monitoring
spec:
  parentRefs:
    - name: envoy-external
      namespace: network
      sectionName: http
  hostnames:
    - uptime.krapulax.dev
  rules:
    - backendRefs:
        - name: uptime-kuma
          namespace: monitoring
          port: 3001
