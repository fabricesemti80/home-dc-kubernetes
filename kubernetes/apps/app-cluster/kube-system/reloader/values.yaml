---
fullnameOverride: reloader
reloader:
  readOnlyRootFileSystem: true
  podMonitor:
    enabled: true
    namespace: "{{ .Release.Namespace }}"
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      cpu: 250m
      memory: 256Mi
