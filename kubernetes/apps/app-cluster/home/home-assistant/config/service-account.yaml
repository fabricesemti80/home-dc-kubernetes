---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: home-assistant
  namespace: home
# The pod's defaultPodOptions already sets automountServiceAccountToken: false;
# keep the SA itself token-free too.
automountServiceAccountToken: false
