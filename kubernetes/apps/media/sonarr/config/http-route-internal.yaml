---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr-internal
  namespace: media
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: http
  hostnames:
    - sonarr.krapulax.home
  rules:
    - backendRefs:
        - name: sonarr
          namespace: media
          port: 8989
