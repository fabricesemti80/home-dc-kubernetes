---
version: '3'

set: [pipefail]
shopt: [globstar]

vars:
  BOOTSTRAP_DIR: '{{.ROOT_DIR}}/bootstrap'
  KUBERNETES_DIR: '{{.ROOT_DIR}}/kubernetes'
  SCRIPTS_DIR: '{{.ROOT_DIR}}/scripts'
  TALOS_DIR: '{{.ROOT_DIR}}/talos'
  PRIVATE_DIR: '{{.ROOT_DIR}}/.private'
  TALOSCONFIG: '{{.ROOT_DIR}}/talos/clusterconfig/talosconfig'
  TF_PROXMOX_DIR: '{{.ROOT_DIR}}/infra/terraform_proxmox'
  TF_CLOUDFLARE_DIR: '{{.ROOT_DIR}}/infra/terraform_cloudflare'
  TF_LOCALDNS_DIR: '{{.ROOT_DIR}}/infra/terraform_localdns'

env:
  KUBECONFIG: '{{.ROOT_DIR}}/kubeconfig'
  SOPS_AGE_KEY_FILE: '{{.ROOT_DIR}}/age.key'
  TALOSCONFIG: '{{.TALOSCONFIG}}'

includes:
  bootstrap: .taskfiles/bootstrap
  docker: .taskfiles/docker
  infrastructure: .taskfiles/infrastructure
  talos: .taskfiles/talos
  template: .taskfiles/template

tasks:

  default: task --list

  validate:
    desc: Validate repository structure and required files
    cmd: bash {{.ROOT_DIR}}/scripts/validate.sh

  deps:
    desc: Install Helm dependencies for the cluster workflow
    cmds:
      - helm plugin list | grep -q 'secrets' || helm plugin install https://github.com/jkroepke/helm-secrets --verify=false
      - helm plugin list | grep -q 'diff' || helm plugin install https://github.com/databus23/helm-diff --verify=false
    preconditions:
      - which helm

  lint:
    desc: Run linters manually with auto-fixing where possible
    cmds:
      - task: tf:fmt
      - shfmt -w -i 2 -s {{.ROOT_DIR}}/scripts/*.sh {{.ROOT_DIR}}/scripts/lib/*.sh {{.ROOT_DIR}}/infra/docker/scripts/*.sh
      - yamllint {{.ROOT_DIR}}
      - shellcheck {{.ROOT_DIR}}/scripts/*.sh {{.ROOT_DIR}}/scripts/lib/*.sh {{.ROOT_DIR}}/infra/docker/scripts/*.sh
    preconditions:
      - which tofu shfmt yamllint shellcheck

  pre-commit:install:
    desc: Install pre-commit git hooks
    cmd: pre-commit install
    preconditions:
      - which pre-commit

  pre-commit:run:
    desc: Run all pre-commit hooks against all files
    cmd: pre-commit run --all-files
    preconditions:
      - which pre-commit

  tf:fmt:
    desc: Format all OpenTofu stacks
    cmds:
      - tofu -chdir={{.TF_PROXMOX_DIR}} fmt -recursive
      - tofu -chdir={{.TF_CLOUDFLARE_DIR}} fmt -recursive
      - |
        if find {{.TF_LOCALDNS_DIR}} -maxdepth 1 -name '*.tf' | grep -q .; then
          tofu -chdir={{.TF_LOCALDNS_DIR}} fmt -recursive
        else
          echo "No localdns OpenTofu files yet; skipping fmt."
        fi
    preconditions:
      - which tofu

  stack:render:
    desc: Render Docker runtime files from Doppler-managed secrets
    cmd: task docker:render

  stack:config:
    desc: Render and validate Docker Compose configuration using Doppler-managed secrets
    cmd: task docker:config

  stack:deploy:
    desc: Render runtime files and start the host-level Docker Compose stack using Doppler-managed secrets
    cmd: task docker:deploy

  infra:provision:
    desc: Apply OpenTofu stacks in execution order
    cmd: task infrastructure:tf-apply

  talos:genconfig:
    desc: Generate Talos machine configuration
    cmd: task talos:generate-config

  talos:bootstrap:
    desc: Bootstrap Talos cluster and join nodes
    cmd: task bootstrap:talos

  apps:bootstrap:
    desc: Bootstrap cluster apps after Talos is healthy
    cmd: task bootstrap:apps

  sync-argo-bootstrap:
    desc: Apply the Argo CD app-of-apps bootstrap resources
    cmd: task infrastructure:argo-sync-bootstrap

  reconcile:
    desc: Force Argo to pull in changes from your Git repository
    cmd: task infrastructure:argo-reconcile

  platform:cilium:
    desc: Install and validate Cilium
    cmd: task infrastructure:platform-cilium

  platform:ingress:
    desc: Validate the Envoy Gateway ingress path
    cmd: task infrastructure:platform-ingress

  platform:argocd:
    desc: Install and validate Argo CD
    cmd: task infrastructure:platform-argocd

  platform:observability:
    desc: Install observability stack
    cmd: task infrastructure:platform-observability

  verify:cluster:
    desc: Run post-bootstrap verification checks
    cmd: task infrastructure:verify

  tf:init:
    desc: Initialize all OpenTofu stacks in execution order
    cmd: task infrastructure:tf-init

  tf:plan:
    desc: Plan all OpenTofu stacks in execution order
    cmd: task infrastructure:tf-plan

  tf:apply:
    desc: Apply all OpenTofu stacks in execution order
    cmd: task infrastructure:tf-apply

  tf:destroy:
    desc: Destroy all OpenTofu stacks in reverse dependency order
    cmd: task infrastructure:tf-destroy

  tf:proxmox:init:
    desc: Initialize the Proxmox/Talos OpenTofu stack
    cmd: task infrastructure:tf-proxmox-init

  tf:proxmox:plan:
    desc: Plan the Proxmox/Talos OpenTofu stack
    cmd: task infrastructure:tf-proxmox-plan

  tf:proxmox:apply:
    desc: Apply the Proxmox/Talos OpenTofu stack
    cmd: task infrastructure:tf-proxmox-apply

  tf:proxmox:destroy:
    desc: Destroy the Proxmox/Talos OpenTofu stack
    cmd: task infrastructure:tf-proxmox-destroy

  tf:cloudflare:init:
    desc: Initialize the Cloudflare OpenTofu stack
    cmd: task infrastructure:tf-cloudflare-init

  tf:cloudflare:plan:
    desc: Plan the Cloudflare OpenTofu stack
    cmd: task infrastructure:tf-cloudflare-plan

  tf:cloudflare:apply:
    desc: Apply the Cloudflare OpenTofu stack
    cmd: task infrastructure:tf-cloudflare-apply

  tf:cloudflare:destroy:
    desc: Destroy the Cloudflare OpenTofu stack
    cmd: task infrastructure:tf-cloudflare-destroy

  tf:localdns:init:
    desc: Initialize the local DNS OpenTofu stack when populated
    cmd: task infrastructure:tf-localdns-init

  tf:localdns:plan:
    desc: Plan the local DNS OpenTofu stack when populated
    cmd: task infrastructure:tf-localdns-plan

  tf:localdns:apply:
    desc: Apply the local DNS OpenTofu stack when populated
    cmd: task infrastructure:tf-localdns-apply

  tf:localdns:destroy:
    desc: Destroy the local DNS OpenTofu stack when populated
    cmd: task infrastructure:tf-localdns-destroy

  cluster:sync-argo:
    desc: Apply the Argo bootstrap resources from this repo
    cmd: task infrastructure:argo-sync-bootstrap

  cluster:reconcile:
    desc: Force Argo to reconcile the cluster apps
    cmd: task infrastructure:argo-reconcile

  mgmt:init:
    desc: Bootstrap the management host from inside this repo
    cmd: bash {{.ROOT_DIR}}/scripts/management-init.sh
    preconditions:
      - test -f {{.ROOT_DIR}}/scripts/management-init.sh

  mgmt:bootstrap:
    desc: Bootstrap the management VM over SSH from this workstation
    cmd: bash {{.ROOT_DIR}}/scripts/bootstrap-management-vm.sh
    preconditions:
      - test -f {{.ROOT_DIR}}/scripts/bootstrap-management-vm.sh
